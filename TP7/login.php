<?php
    echo $pseudo;
    echo $pwd;
    // session_start();
    // session_register();
    // session_unregister();
    // session_is_registered();
    // session_id();    
    // session_name();
    // session_unset();
    // session_destroy();
?>